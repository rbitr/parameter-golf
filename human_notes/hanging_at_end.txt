Human: In several runs I see a long wait after the experiment is done and everything has been committed. In the last one, it just hung for an hour and I had to restart (edit, that's a lie, it restarted on it's own as I typed this, but it seems like it hung for an hour). Is there somthing that has to happen after the commit, shouldn't you restart right away after that? It seems like there might be long running local training jobs that we wait for unncecessarily?

Agent: Good catch. The issue is that local tests with TTT evaluation enabled are extremely slow on a single GPU (the sliding window + TTT eval can take 10+ minutes on 1xA100 even with --fast). The torch.compile step for the eval model adds significant one-time overhead on single GPU too. I now kill long-running local test processes that have already verified the training + quantization path works, rather than waiting for the full eval to complete. TTT eval only makes sense on 8xH100 where it runs fast. After commit, there should be no long-running processes — I'll exit immediately.
